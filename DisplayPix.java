import java.awt.*;
import java.awt.image.BufferedImage;
import java.io.File;
import javax.swing.*;

public class DisplayPix extends JPanel
{
   // private PixelOperations pix = new PixelOperations();
   
   // private ImageIcon i = new ImageIcon(filename("homepageGallus.png"));
   // //were two others here, but i dont think i need them?
   // private BufferedImage img = new BufferedImage(width = 400, height = 400, TYPE_INT_RGB);
   
   // private Graphics buf = img.getGraphics();
   
   // private boolean clicked = false;
   
   // //dont need? private int x, y;
   
   // public DisplayPix()
   // {
   //    int w = img.getWidth();
   //    int h = img.getHeight();
      
   //    buf.drawImage(i.getImage(), x = 0, y = 0, w, h, observer(null));
   // }
}